# Discord24

A bot for playing the game of 24 on Discord.

### Setup

```
npm install
```

Move/copy `config.json.template` to `config.json`, and edit in your bot token.  
Move/copy `scores.json.DO_NOT_EDIT` to `scores.json`.

### Run

Start the bot with `npm start`.
